@extends('layouts.app')

@section('content')
<!-- Hero Section -->
<section class="relative bg-gray-100 min-h-screen flex items-center">
    <div class="container mx-auto px-4">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
            <div class="space-y-8">
                <h1 class="text-6xl md:text-7xl font-bold leading-tight">
                    DEFINE YOUR<br>
                    SIGNATURE<br>
                    LOOK
                </h1>
                <p class="text-lg text-gray-600 leading-relaxed">
                    Discover Timeless elegance for the key<br>
                    modern outfits.
                </p>
                <div class="space-y-4">
                    <a href="{{ route('shop') }}" class="inline-block px-8 py-4 bg-black text-white font-semibold hover:bg-gray-800 transition">
                        SHOP NOW
                    </a>
                    <div>
                        <a href="{{ route('shop') }}" class="text-sm underline hover:no-underline">
                            View More Overviews
                        </a>
                    </div>
                </div>
            </div>
            <div class="relative">
                @if($featuredProduct && $featuredProduct->primaryImage)
                    <img src="{{ asset('storage/' . $featuredProduct->primaryImage->image_path) }}" 
                         alt="{{ $featuredProduct->name }}" 
                         class="w-full h-auto object-cover">
                @else
                    <div class="w-full h-96 bg-gray-300 flex items-center justify-center">
                        <span class="text-gray-500">No Featured Product</span>
                    </div>
                @endif
            </div>
        </div>
    </div>
</section>

<!-- Brand Partners -->
<section class="bg-black py-8">
    <div class="container mx-auto px-4">
        <div class="flex justify-around items-center flex-wrap gap-8 text-white text-2xl font-bold">
            <span>EP</span>
            <span>H&M</span>
            <span>LEVI'S</span>
            <span>ACCESSORIES</span>
            <span>GILAI</span>
        </div>
    </div>
</section>

<!-- Most Popular Products -->
<section class="py-20 bg-white">
    <div class="container mx-auto px-4">
        <div class="mb-12">
            <p class="text-sm text-gray-500 mb-2">— OUR TRENDING SHOES</p>
            <div class="flex justify-between items-end">
                <div>
                    <h2 class="text-4xl font-bold mb-2">Most Popular Products</h2>
                    <p class="text-gray-600">Lorem ipsum dolor sit amet consectetur adipiscing elit</p>
                </div>
                <a href="{{ route('shop') }}" class="px-6 py-2 border-2 border-black hover:bg-black hover:text-white transition">
                    EXPLORE
                </a>
            </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
            @forelse($popularProducts->take(4) as $product)
            <div class="group">
                <a href="{{ route('product.detail', $product->slug) }}" class="block relative overflow-hidden bg-gray-100 aspect-square mb-4">
                    @if($product->primaryImage)
                        <img src="{{ asset('storage/' . $product->primaryImage->image_path) }}" 
                             alt="{{ $product->name }}" 
                             class="w-full h-full object-cover group-hover:scale-110 transition duration-500">
                    @else
                        <div class="w-full h-full flex items-center justify-center">
                            <span class="text-gray-400">No Image</span>
                        </div>
                    @endif
                    @if($product->is_new)
                        <span class="absolute top-4 right-4 bg-black text-white text-xs px-3 py-1">NEW</span>
                    @endif
                    <button class="absolute bottom-4 right-4 w-10 h-10 bg-black text-white rounded-full flex items-center justify-center opacity-0 group-hover:opacity-100 transition">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                        </svg>
                    </button>
                </a>
                <div>
                    <h3 class="font-semibold mb-1">{{ $product->name }}</h3>
                    <p class="text-gray-600">IDR {{ number_format($product->final_price, 0, ',', '.') }}</p>
                </div>
            </div>
            @empty
            <div class="col-span-4 text-center py-12 text-gray-500">
                Belum ada produk tersedia
            </div>
            @endforelse
        </div>
    </div>
</section>

<!-- Curated Categories -->
<section class="py-20 bg-gray-50">
    <div class="container mx-auto px-4">
        <div class="flex justify-between items-center mb-12">
            <h2 class="text-4xl font-bold">OUR CURATED CATEGORIES</h2>
            <button class="w-12 h-12 border-2 border-black hover:bg-black hover:text-white transition flex items-center justify-center">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
            </button>
        </div>

        <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
        @php
            $categories = \App\Models\Category::with(['products' => function($query) {
                $query->with('primaryImage')->where('is_active', true)->take(1);
            }])
            ->whereIn('id', [1,2,3,4])
            ->take(4)
            ->get();
        @endphp
            
            @forelse($categories as $category)
            @if($category->products->isNotEmpty())
                @php $product = $category->products->first(); @endphp
                <div class="group relative overflow-hidden">
                    <a href="{{ route('shop', ['category' => $category->id]) }}" class="block aspect-square bg-gray-200 overflow-hidden">
                        @if($product->primaryImage)
                            <img src="{{ asset('storage/' . $product->primaryImage->image_path) }}" 
                                alt="{{ $category->name }}" 
                                class="w-full h-full object-cover group-hover:scale-110 transition duration-500">
                        @else
                            <div class="w-full h-full flex items-center justify-center">
                                <span class="text-gray-400">{{ $category->name }}</span>
                            </div>
                        @endif
                        <div class="absolute bottom-4 left-4 right-4 bg-white bg-opacity-90 p-3">
                            <h3 class="font-semibold">{{ $product->name }}</h3>
                            <p class="text-sm text-gray-600">IDR {{ number_format($product->final_price, 0, ',', '.') }}</p>
                        </div>
                        @if($product->is_new)
                            <span class="absolute top-4 right-4 bg-black text-white text-xs px-3 py-1">NEW</span>
                        @endif
                    </a>
                </div>
            @endif
        @empty
            <div class="col-span-4 text-center py-12 text-gray-500">
                Belum ada kategori tersedia
            </div>
        @endforelse
        </div>
    </div>
</section>

<!-- Best Sellers -->
<section class="py-20 bg-white">
    <div class="container mx-auto px-4">
        <div class="flex justify-between items-center mb-12">
            <h2 class="text-4xl font-bold">BEST SELLERS</h2>
            <button class="w-12 h-12 border-2 border-black hover:bg-black hover:text-white transition flex items-center justify-center">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
            </button>
        </div>

        <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
            @forelse($bestSellingProducts->take(8) as $product)
            <div class="group">
                <a href="{{ route('product.detail', $product->slug) }}" class="block relative overflow-hidden bg-gray-100 aspect-square mb-4">
                    @if($product->primaryImage)
                        <img src="{{ asset('storage/' . $product->primaryImage->image_path) }}" 
                             alt="{{ $product->name }}" 
                             class="w-full h-full object-cover group-hover:scale-110 transition duration-500">
                    @else
                        <div class="w-full h-full flex items-center justify-center">
                            <span class="text-gray-400">No Image</span>
                        </div>
                    @endif
                    @if($product->has_discount)
                        <span class="absolute top-4 left-4 bg-red-500 text-white text-xs px-3 py-1">SALE</span>
                    @endif
                </a>
                <div>
                    <h3 class="font-semibold mb-1">{{ $product->name }}</h3>
                    <div class="flex items-center space-x-2">
                        <p class="font-bold">IDR {{ number_format($product->final_price, 0, ',', '.') }}</p>
                        @if($product->has_discount)
                            <p class="text-sm text-gray-500 line-through">IDR {{ number_format($product->price, 0, ',', '.') }}</p>
                        @endif
                    </div>
                </div>
            </div>
            @empty
            <div class="col-span-4 text-center py-12 text-gray-500">
                Belum ada produk tersedia
            </div>
            @endforelse
        </div>
    </div>
</section>

<!-- Customer Testimonials -->
<section class="py-20 bg-gray-50">
    <div class="container mx-auto px-4">
        <div class="text-center mb-12">
            <h2 class="text-4xl font-bold mb-4">CUSTOMER TESTIMONIALS</h2>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto">
            @forelse($testimonials as $testimonial)
            <div class="bg-white p-8">
                <div class="flex items-center mb-4">
                    @if($testimonial->customer_photo)
                        <img src="{{ asset('storage/' . $testimonial->customer_photo) }}" 
                             alt="{{ $testimonial->customer_name }}" 
                             class="w-16 h-16 rounded-full object-cover mr-4">
                    @else
                        <div class="w-16 h-16 rounded-full bg-gray-300 mr-4"></div>
                    @endif
                    <div>
                        <h4 class="font-semibold">{{ $testimonial->customer_name }}</h4>
                        <div class="flex text-yellow-400 text-sm">
                            @for($i = 0; $i < $testimonial->rating; $i++)
                                ★
                            @endfor
                        </div>
                    </div>
                </div>
                <p class="text-gray-600 leading-relaxed mb-4">{{ $testimonial->testimonial }}</p>
                <div class="text-right">
                    <svg class="w-8 h-8 text-gray-300 ml-auto" fill="currentColor" viewBox="0 0 24 24">
                        <path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10h-9.983zm-14.017 0v-7.391c0-5.704 3.748-9.57 9-10.609l.996 2.151c-2.433.917-3.996 3.638-3.996 5.849h3.983v10h-9.983z"/>
                    </svg>
                </div>
            </div>
            @empty
            <div class="col-span-2 text-center py-12 text-gray-500">
                Belum ada testimoni
            </div>
            @endforelse
        </div>
    </div>
</section>
@endsection 